import { ComingSoon } from '@/components/job/ComingSoon';
export default function JobScope({ job }) {
  return <ComingSoon title="Scope" description="Define the scope of work per room and category." />;
}